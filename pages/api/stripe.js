//inside the api folder in node js, all the files are used to create the api for connecting the front end to the back end

import Stripe from 'stripe'; //importing stripe library to use its features 
