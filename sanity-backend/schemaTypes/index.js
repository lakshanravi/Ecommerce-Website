import product from "./product"
import banner from "./banner"

//now we will export the schema types to be used in sanity studio
//this is the array of schema types
export const schemaTypes = [product, banner]
